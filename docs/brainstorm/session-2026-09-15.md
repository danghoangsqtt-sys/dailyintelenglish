# Brainstorm Session — Daily Intel English Studio (Phase 4 Planning)
**Date:** 2026-09-15
**Status:** Scope locked — ready for `/vp-crystallize` or `/vp-evolve` to formalize Phase 4 in `ROADMAP.md`
**workflow_version:** 2.19.0
**Session ID:** session-2026-09-15
**Relates to:** `docs/brainstorm/session-2026-09-10.md` (original crystallization), `docs/brainstorm/session-2026-09-14.md` (UI redesign direction — this session continues that direction's remaining scope)

---

## Trigger

User: v1.0.0-beta vừa hoàn tất (tất cả 3 phase ROADMAP gốc xong ngày 2026-09-15, Day 6/21).
Yêu cầu: xác định kế hoạch tiếp theo cần thực hiện.

## Candidate review (PM, trước khi hỏi user)

Rà lại các hạng mục đã biết từ `TRACKER.md`/`HANDOFF.json`/session 14/09 trước khi đặt câu hỏi,
thay vì để user tự nhớ lại:

1. Progress cancellation (Task 2.2, hoãn) — cần kiến trúc background-job/cancel mới, mọi
   route generate hiện là request/response đồng bộ.
2. LivePortrait lip-sync thật (Task 1.7, hoãn) — avatar-upload đã xong, model lip-sync
   thật chưa tích hợp; quy mô tương đương investigation OmniVoice đã quyết định bỏ.
3. 7/9 trang UI redesign còn lại (session 14/09) — Task 2.4 "Slice 1" mới làm Dashboard +
   Script; Learning/TTS/Video/Thumbnail/YouTube/Music Library/Step1-Config vẫn ở cấu trúc
   cũ (kiểm tra thật: `grep -c "shell.js" frontend/pages/*.html` xác nhận chỉ
   `step2_script.html` dùng `WorkspaceShell`).
4. Gemini 503 chưa retry — **đã kiểm tra lại mã nguồn thật, phát hiện ĐÃ ĐƯỢC FIX** cùng
   ngày 14/09 (`_RETRYABLE_STATUS_CODES = (429, 503)` có mặt ở cả 4 service Gemini) —
   ghi chú "phát hiện phụ" trong session cũ đã lỗi thời, không phải gap thật nữa.
5. `news`-genre kéo lệch CEFR (Task 2.1b) — 4/18 mẫu BORDERLINE, không phải lỗi, chỉ là
   điểm tinh chỉnh prompt tùy chọn.

User: không có ý tưởng tính năng mới, chỉ muốn xử lý các hạng mục có sẵn; để PM đề xuất
phương án vì "không biết chọn nội dung nào".

## PM Recommendation (trình bày cho user trước khi chốt)

| Hạng mục | Effort | Rủi ro | Giá trị thực | Khuyến nghị |
|---|---|---|---|---|
| Tinh chỉnh CEFR `news` | Nhỏ | Thấp | Vừa | Làm, gộp Phase 4 |
| UI redesign 7 trang còn lại | Vừa | Thấp (đã có pattern Task 2.4) | Cao — đúng phàn nàn gốc của user | Làm, Phase 4 |
| Progress cancellation | Lớn | Vừa (đổi kiến trúc) | Chưa rõ — app chạy local 1 user, pipeline đã nhanh (demo full pipeline ~226s) | Tiếp tục hoãn |
| LivePortrait lip-sync thật | Rất lớn | Cao | Chưa rõ — nguy cơ lặp kết cục "không đáng đầu tư" như OmniVoice | Tiếp tục hoãn |

User: đồng ý toàn bộ đề xuất, theo đúng thứ tự (4.1 quick fix trước, 4.2 UI redesign sau).

---

## Decisions

| Topic | Decision | Rationale |
|-------|----------|-----------|
| Phạm vi Phase 4 | 2 task: (4.1) tinh chỉnh prompt CEFR `news`, (4.2) UI Redesign Slice 2 — 7 trang còn lại | Cả 2 đều rủi ro thấp, giá trị rõ ràng; không đưa Progress cancellation/LivePortrait vào phase này |
| Thứ tự | 4.1 trước (nhỏ, nhanh), 4.2 sau | 4.1 không phụ thuộc 4.2; làm trước để dọn sạch trước khi vào việc lớn hơn |
| Nhịp độ Task 4.2 | Chia nhỏ theo từng trang/cặp trang (task con riêng mỗi trang), giống cách Task 2.3 đã chia (2.3b/c/d/e) | Task 2.4 chỉ 2 trang đã cần 49 test mới — làm cả 7 trang một lần rủi ro cao hơn nhiều so với chia nhỏ và kiểm tra kỹ từng bước |
| Progress cancellation | Tiếp tục hoãn, không đưa vào Phase 4 | Cần kiến trúc job/cancel mới hoàn toàn; lợi ích thực tế chưa cấp thiết cho app 1-user local |
| LivePortrait lip-sync | Tiếp tục hoãn, không đưa vào Phase 4 | Quy mô nghiên cứu tương đương OmniVoice (đã quyết định không làm 2026-09-13); rủi ro lặp lại kết cục tương tự |
| Project meta intake (FEAT-009) | Bỏ qua (waived) | User chọn bỏ qua — không ảnh hưởng đến nội dung kế hoạch Phase 4; có thể thiết lập sau nếu cần |

## Phases

### Phase 4 — Post-v1.0.0-beta Polish (mới, chưa có trong ROADMAP.md gốc)

- **Task 4.1 — CEFR `news`-genre prompt tuning**
  - Tinh chỉnh `prompts/script/news.txt` (và/hoặc block CEFR liên quan) để giảm độ khó
    idiom/grammar về ngang các genre khác cùng level — vấn đề cụ thể: A2/B1/B2 × `news`
    hiện BORDERLINE trong đánh giá thật 18 mẫu (Task 2.1b).
  - Verify: chạy lại `scripts/generate_cefr_review_samples.py` cho ít nhất A2/B1/B2 ×
    `news`, so sánh với baseline 18-mẫu đã có.

- **Task 4.2 — UI Redesign Slice 2 (7 trang còn lại)**
  - Áp dụng shell 3 vùng (đã duyệt ở session 14/09, đã có mockup tĩnh tại
    `.viepilot/ui-direction/2026-09-14/pages/`) cho: Learning, TTS, Video, Thumbnail,
    YouTube, Music Library, Step1-Config.
  - Music Library và Step1-Config **không** dùng shell 3 vùng (quyết định đã có từ
    session 14/09 — Music Library là trang tiện ích dùng chung, Step1 là form thuần).
  - Timeline chỉ xuất hiện ở Learning/TTS/Video nếu áp dụng — **cần xác nhận lại ở
    task-card**: session 14/09 quyết định timeline chỉ ở Script/TTS/Video (không có
    Learning); Learning dùng inspector cho chi tiết từ vựng thay vì track timeline.
  - Chia thành task con theo từng trang/cặp trang (thứ tự đề xuất theo pipeline: Learning
    → TTS → Video → Thumbnail → YouTube → Music Library → Step1-Config), PM xác nhận thứ
    tự cụ thể khi viết task card doc-first.
  - Mỗi task con cần: giữ nguyên async-safety đã test (double-submit lock, coalesced
    autosave, dirty-navigation guard), viết Playwright test mới tương ứng, không phá vỡ
    533 test hiện có.

## Deferred (không thuộc Phase 4, giữ nguyên trạng thái đã ghi trong TRACKER.md)

- Progress cancellation (Task 2.2) — cần kiến trúc background-job/cancel mới.
- Real LivePortrait lip-sync (Task 1.7) — nghiên cứu+build lớn, chưa bắt đầu.

## Open questions

1. Thứ tự cụ thể và cách gộp/tách task con của Task 4.2 (ví dụ: Learning+TTS chung 1 task
   hay tách riêng) — để PM quyết định khi viết task card doc-first theo AR-06, không chốt
   cứng ở đây.
2. Timeline có nên xuất hiện ở trang Learning hay không (session 14/09 nói không, nhưng
   chưa re-confirm cho session này) — xác nhận lại khi viết task-card 4.2.

## Next steps

1. `/vp-crystallize` hoặc `/vp-evolve` để đưa Phase 4 (2 task) vào `.viepilot/ROADMAP.md`
   chính thức, cùng phase-state scaffold `.viepilot/phases/04-post-beta-polish/`.
2. `/vp-auto` để bắt đầu Task 4.1 (nhỏ, làm trước) rồi Task 4.2 (chia nhỏ theo trang).

## Update (cùng ngày) — Phase 4 tiến độ + quyết định giao Codex làm Task 4.2b

Đã thực hiện qua `/vp-auto` (không cần `/vp-crystallize` riêng — PM tự scaffold Phase 4
trực tiếp, cùng cách Phase 3 đã mở trước đó):
- **Task 4.1** (tinh chỉnh CEFR `news`) — xong, cải thiện một phần, báo cáo trung thực
  (không phải fix triệt để). Xem `tasks/task-4.1.md`.
- **Task 4.2a** (trang Learning) — xong, PM tự implement. Thêm inspector chỉ đọc, giữ
  nguyên autosave/inline-edit. Phát hiện+sửa 1 lỗi thật (`step_nav` thiếu
  `variant: "workflow"`) trong lúc verify, và thêm 1 lỗi thật nữa qua `/vp-audit` sau đó
  (`renderInspector()` không được gọi lại sau khi sửa field đang chọn). Xem
  `tasks/task-4.2a.md`.

**Quyết định mới**: user báo Codex đã hồi phục quota, muốn PM (Claude Code) chuyển sang
vai trò kiểm soát dự án (giống hợp đồng AR-06 đã dùng ở Phase 1) — Codex làm
Implementer cho **Task 4.2b (trang TTS Audio Studio, `/step4`)**, PM viết task card
doc-first + review/accept, không tự code.

- Task card đã viết: `.viepilot/phases/04-post-beta-polish/tasks/task-4.2b.md` — bao
  gồm nghiên cứu thật cấu trúc `step4_tts.html`/`.js` hiện tại, các quyết định đã chốt
  (shell 3 vùng + timeline 3 track Script/Voice/Music — đúng quyết định TTS có timeline
  từ session 14/09), và một thiết kế inspector đề xuất (Codex cần xác nhận/tinh chỉnh
  trước khi code, đúng quy trình AR-06 bước 2-3).
- Sửa một lỗi tài liệu thật trong lúc chuẩn bị: `docs/CODEX_CODE_PROMPT.md` từng hard-code
  đường dẫn `.viepilot/phases/01-full-feature-build/tasks/` — có thể khiến Codex tìm
  nhầm thư mục phase. Đã sửa thành placeholder tổng quát + ví dụ đường dẫn Phase 4 thật.
- Prompt thật để gửi Codex được đưa trực tiếp trong chat (không lưu file riêng, tránh
  trùng lặp với `docs/CODEX_CODE_PROMPT.md`).

## Update (cùng ngày) — Tính năng mới: Việt hóa giao diện (Task 4.3, mới)

User: "tôi cần giao diện việt hóa". Xác nhận trước khi lên kế hoạch: hiện **chưa có hạ
tầng i18n nào** — toàn bộ text tiếng Anh viết cứng trực tiếp trong HTML/JS ở cả 9 trang
(kiểm tra thật: ~170 chỗ gán `textContent`/`innerHTML`/label tĩnh rải rác trong
`frontend/static/js/*.js`, chưa tính text tĩnh trong các file `.html`).

### Quyết định

| Câu hỏi | Quyết định | Lý do |
|---|---|---|
| Toggle EN/VI hay thay thẳng? | **Thay thẳng sang tiếng Việt**, không cần toggle | App cá nhân 1 người dùng, không cần hệ thống i18n đầy đủ — đơn giản hơn, rủi ro thấp hơn |
| Thuật ngữ chuyên môn (CEFR, genre, accent) | **Giữ nguyên tiếng Anh** (A1-C2, `small_talk`, `american`, v.v.) | CEFR là chuẩn quốc tế; genre/accent là giá trị gửi thẳng lên API, dịch sẽ cần map ngược — không đáng, chỉ nhãn hiển thị (label) có thể chú thích tiếng Việt |
| Phạm vi/nhịp độ | **Chia nhỏ theo từng trang** | Giống cách Task 4.2/2.3 đã làm, rủi ro thấp hơn làm 1 lần cả 9 trang |
| Thứ tự với Task 4.2 (UI redesign shell) | **Làm SAU khi Task 4.2 xong hết** (7/7 trang) | Nếu Việt hóa trước, mỗi trang khi chuyển sang shell mới sẽ phải sửa lại text tiếng Việt lần 2 — lãng phí công |

### Phạm vi loại trừ (đã rõ, không cần hỏi lại)

- Nội dung do AI tạo ra cho từng dự án (script, vocabulary, quiz...) — **không** thuộc
  phạm vi này, vì đó là sản phẩm dạy tiếng Anh, bản chất phải là tiếng Anh. Đây chỉ nói
  về giao diện ỨNG DỤNG (nút bấm, nhãn, tiêu đề, thông báo lỗi).

## Phases

- **Phase 4, Task 4.3 (mới)**: Việt hóa giao diện ứng dụng, chia theo 9 trang, **phụ
  thuộc Task 4.2 hoàn tất trước** (không bắt đầu tới khi 7/7 trang Task 4.2 xong). PM sẽ
  viết task card doc-first cho từng trang khi tới lượt, theo AR-06.

## Next steps

1. Tiếp tục `/vp-auto` cho Task 4.2b (trang TTS, đang chờ Codex) và các trang còn lại
   của Task 4.2.
2. Sau khi Task 4.2 xong toàn bộ 7/7 trang: `/vp-auto` mở Task 4.3, viết task card
   doc-first cho từng trang, Việt hóa lần lượt.
