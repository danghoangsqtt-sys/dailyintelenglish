You are writing ONE section of a {{ genre }} podcast episode at CEFR
{{ cefr_level }} about "{{ topic }}" for Daily Intel English Studio.

## Episode Title
{{ outline_title }}

## This Section
- Section {{ section_index }} of {{ section_count }}
- Objective: {{ objective }}
- Target length: between {{ min_words }} and {{ max_words }} spoken words —
  count your words before answering and stay inside this range.
{% if prior_summary %}
- What happened just before this section: {{ prior_summary }}
  Continue naturally from this — do not repeat it, do not restart the topic.
{% else %}
- This is the FIRST section of the episode — open naturally (a brief intro is
  fine here, but do not pad with unrelated small talk).
{% endif %}
{% if is_last_section %}
- This is the LAST section of the episode — close it out naturally (a brief
  outro/wrap-up is appropriate here), and end it with a short spoken sign-off
  to the audience, not just a final piece of advice or a call to action alone.
{% endif %}
{% if avoid_phrases %}

## Avoid Repeating These Phrases
These phrases have already appeared multiple times earlier in this episode —
do not reuse them:
{% for phrase in avoid_phrases %}
- "{{ phrase }}"
{% endfor %}
{% endif %}

## Speakers
Each speaker is identified by a short alias. Use ONLY the alias in your
output — never a name, never an id.
{% for speaker in speakers %}
- alias: S{{ loop.index }} | name: {{ speaker.name }} | {{ speaker.gender }}, {{ speaker.accent }} accent
{% endfor %}
{% if num_speakers == 1 %}

**Solo format:** only one speaker — write this section as continuous monologue
from {{ speakers[0].name }}, not a dialogue.
{% else %}

**Turn-taking:** alternate speakers by default. No single speaker may ever
deliver more than {{ max_consecutive_lines }} consecutive lines before another
speaker responds.
{% endif %}

## CEFR {{ cefr_level }} Constraints
{{ cefr_constraints }}

## Precedence: CEFR Constraints vs. Language Feature Toggles
The CEFR constraints above define a CEILING, not a requirement. Whether you may
use a category at all is controlled ONLY by the list below:
{% for key, enabled in language_features.items() if enabled %}
- {{ key.replace('_', ' ') }} — allowed, never above the CEFR ceiling
{% endfor %}
{% if not (language_features.values() | select | list) %}
- None requested — use plain, level-appropriate language only.
{% endif %}

## Rules
1. Stay on topic: every line must relate to "{{ topic }}".
2. Do not invent a speaker — use only the alias values listed above.
3. Do not number or label lines with an `id` field — the app assigns line IDs.
4. Vary how speakers agree or react to each other. Never open more than one turn in the whole episode with the same stock agreement phrase — for example, always praising what the other speaker just said in the same way before adding your own view. React differently each time.
5. State the episode's main point or takeaway freshly, in your own words, each time it comes up. Never repeat the same summary or takeaway sentence, word-for-word or nearly so, in more than one section.{% if is_last_section %} The one exception is the final section's sign-off (see above): a brief, one-sentence recap of the takeaway is allowed there.{% endif +%}
6. Do not restate the same sentence or claim twice within this section itself, even reworded in a way that reuses most of the same wording.

## Output Format
Return ONLY a JSON array (no markdown fences, no commentary) where each element
has this shape:

```json
{
  "speaker": "the exact alias (e.g. S1) of the speaker from the Speakers list above",
  "text": "The line of dialogue.",
  "language_notes": {
    "collocations": ["..."],
    "idioms": ["..."],
    "grammar_point": "short label, e.g. Present Simple — greeting"
  }
}
```
