Your previous answer for this section did not pass validation. Fix EVERY issue
listed below and return a corrected version — do not explain the fix, just
return the corrected JSON.

## Section Being Repaired
- Objective: {{ objective }}
- Target length: about {{ target_words }} spoken words (stay within ±15%)
- Measured word count of your previous answer: {{ measured_words }}
  ({{ "+" if delta > 0 else "" }}{{ delta }} vs. target)
{% if delta > 0 %}
- Your previous answer was OVER length by {{ delta }} words. Remove or shorten
  specific existing lines until the total reaches the target — do not rewrite
  the section from scratch (a full rewrite risks landing over length again).
{% elif delta < 0 %}
- Your previous answer was UNDER length by {{ -delta }} words. Add or expand
  specific lines until the total reaches the target.
{% endif %}

## Speakers (use only these aliases)
{% for speaker in speakers %}
- alias: S{{ loop.index }} | name: {{ speaker.name }}
{% endfor %}

## Validation Errors To Fix
{% for error in errors %}
- {{ error }}
{% endfor %}

Also avoid reusing the same stock agreement phrase or the same summary/
takeaway sentence you (or your previous answer above) already used elsewhere
in this section or episode, even in reworded form.

## Your Previous (Invalid) Answer
```json
{{ previous_output }}
```

## Output Format
Return ONLY a corrected JSON array (no markdown fences, no commentary), same
shape as before: each element is `{"speaker": "...", "text": "...",
"language_notes": {"collocations": [...], "idioms": [...], "grammar_point": "..."}}`.
`"speaker"` must be one of the aliases listed above (e.g. `"S1"`). Do not
include an `id` field.
