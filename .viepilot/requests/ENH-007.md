# ENH: ARCHITECTURE.md describes dropped/deferred features as active and diverges from its own Mermaid sidecar

## Meta
- **ID**: ENH-007
- **Type**: Enhancement
- **Status**: done
- **Priority**: medium
- **Created**: 2026-09-18
- **Reporter**: Codex (parallel read-only vp-audit pass, requested alongside PM's own audit)
- **Assignee**: unassigned
- **Source**: Codex's independent read-only audit, independently re-verified by PM

## Summary
Three confirmed inaccuracies in `.viepilot/ARCHITECTURE.md`:

1. **Line 11**: the System Overview text diagram still says
   `↕ REST API / WebSocket (streaming)`. The real implementation is pure
   synchronous polling everywhere — confirmed via `app/api/audio.py`'s own header
   docstring, which explicitly documents `GET /status` as "a plain polling GET, not
   real Server-Sent Events... a deliberate, documented deviation," and no WebSocket
   route exists anywhere in `app/`.
2. **Around line 124-133**: the Mermaid data-flow diagram's Step 4/Step 5 subgraphs
   show `E2[OmniVoice GPU\ngenerate per line]` and `F3[Lips-sync Avatar\nLivePortrait]`
   as active pipeline steps. In reality, Edge TTS is the sole official TTS engine
   (OmniVoice GPU inference was explicitly decided against, per
   `.viepilot/HANDOFF.json`'s `tech_stack.tts_primary` note and
   `.viepilot/TRACKER.md`'s Decision Log), and LivePortrait lip-sync was never
   implemented (only avatar image upload exists — Sub-task 1.7c). Both remain
   correctly logged as deferred in TRACKER.md's Known Issues, but ARCHITECTURE.md's
   own diagram still shows them as if shipped and active.
3. **The embedded `system-overview` Mermaid diagram vs. its sidecar file**
   (`.viepilot/architecture/system-overview.mermaid`): PM diffed them directly and
   confirmed exactly 2 diverging edges — the sidecar includes `LCS`
   (LearningContentService) in both the router fan-out edge
   (`AR --> SS & LCS & TS & AS & VS & THS & PS & YTS`) and the Gemini-dependency edge
   (`SS & LCS --> GEM`); the embedded version in ARCHITECTURE.md is missing `LCS`
   from both.

## Details
- **Tier**: 2 (documentation accuracy for the canonical architecture reference)
- **Affected files**: `.viepilot/ARCHITECTURE.md`,
  `.viepilot/architecture/system-overview.mermaid`
- **Verification method**: read the relevant sections directly; cross-referenced
  against `app/api/audio.py`'s own docstring, `.viepilot/HANDOFF.json`'s tech_stack,
  `.viepilot/TRACKER.md`'s Decision Log/Known Issues; diffed the embedded vs.
  sidecar Mermaid source directly.

## Acceptance Criteria
- [x] Remove or correct the WebSocket claim in the System Overview text diagram
- [x] Update the data-flow Mermaid diagram to show Edge TTS as the active engine and
      mark LivePortrait lip-sync as not-yet-implemented
- [x] Sync the embedded system-overview Mermaid block with its sidecar file
- [ ] Re-run vp-audit: this finding no longer appears (not yet re-run since the fix)

## PM Acceptance
Verified 2026-09-18 under Phase 10 / Task 10.2: all 3 inaccuracies fixed. Also found
and fixed the same LivePortrait inaccuracy in the separate Module Dependencies
diagram (both embedded and sidecar copies) while addressing item 2 — confirmed via a
repo-wide grep that no `app/` code references LivePortrait except docstrings
explaining it's not built. Confirmed the embedded system-overview block now matches
its sidecar byte-for-byte via direct diff. **Accepted.**

## Related
- Files: `.viepilot/ARCHITECTURE.md`, `.viepilot/architecture/system-overview.mermaid`,
  `.viepilot/architecture/module-dependencies.mermaid`
- Cross-reference: `.viepilot/HANDOFF.json` (`tech_stack`), `.viepilot/TRACKER.md`
  (Decision Log, Known Issues)
