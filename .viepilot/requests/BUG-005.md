# BUG: Reject explicit null in Learning Content updates

## Meta
- **ID**: BUG-005
- **Type**: Bug
- **Status**: done
- **Priority**: high
- **Created**: 2026-09-11
- **Reporter**: vp-audit (auto-logged)
- **Assignee**: AI
- **Source**: auto-logged by vp-audit Tier 3

## Summary
`LearningPackUpdate` accepts explicit JSON null. The service serializes it as `null`, replacing a list-valued section and violating the LearningPack response shape.

## Details
- **Tier**: 3
- **Audit date**: 2026-09-11
- **Affected files**: `app/models/learning.py`, `app/services/learning_service.py`, `tests/test_learning_api.py`

## Acceptance Criteria
- [x] Explicit null returns 422 for every Learning Content section
- [x] Omitted fields remain unchanged and empty lists remain valid
- [x] Add service and HTTP regression tests
- [x] Re-run vp-audit: this finding no longer appears

## PM Acceptance
Verified 2026-09-11: `LearningPackUpdate` (`app/models/learning.py:67-78`) rejects explicit null via `model_validator(mode="before")`, mirrors `ProjectUpdate` pattern. Service-side redundant check in `learning_service.py:261-265`. Omitted fields pass through unchanged; empty lists valid (no `min_length`). Test suite green (190 backend tests). **Accepted.**

## Related
- Files: `app/models/learning.py`, `app/services/learning_service.py`, `tests/test_learning_api.py`
- Dependencies: —

## Implementer Evidence (Awaiting PM Acceptance)
Added `@model_validator(mode="before")` on `LearningPackUpdate` in `app/models/learning.py` to reject explicit JSON nulls with HTTP 422 while allowing omitted fields. In `app/services/learning_service.py::update_learning_content`, added validation raising `ValueError` on explicit nulls and preserving empty lists. Added comprehensive unit and HTTP regression tests in `tests/test_learning_api.py`.
