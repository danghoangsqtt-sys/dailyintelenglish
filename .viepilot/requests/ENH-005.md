# ENH: Per-page inline CSS fragmentation causes verified visual drift from the shared stylesheet

## Meta
- **ID**: ENH-005
- **Type**: Enhancement
- **Status**: done (concrete drift only — see "Scope of the fix" below)
- **Priority**: medium
- **Created**: 2026-09-17
- **Reporter**: vp-audit (auto-logged)
- **Assignee**: PM (Claude Code), self-implemented under Phase 8 / Task 8.1
- **Source**: auto-logged by vp-audit Tier 3, independently verifying a Gemini-authored external audit finding (`C:\Users\Admin\Documents\audit_chuyensau_dailyintelenglish`)

## Summary
7 of 9 frontend pages carry their own `<style>` block: 992 total lines of per-page
inline CSS versus 227 lines in the shared `frontend/static/css/style.css` — roughly
4.4x more CSS living scattered across pages than centralized. This alone would just be
a maintainability concern, but independent verification found **concrete, real visual
drift**, not merely duplication:

- Shared `style.css` defines disabled-button opacity as `.btn[disabled] { opacity: .58; ... }`.
- `music_library.html` locally redefines it as `.btn:disabled { opacity: 0.55; ... }`
  (different selector mechanism, different opacity value).
- `step7_youtube.html` locally redefines it again as
  `.btn[aria-disabled="true"] { opacity: 0.55; ... }` (a third selector mechanism, same
  0.55 value, still inconsistent with the shared 0.58).
- `step6_thumbnail.html` overrides `.btn-sm { padding: 7px 11px; font-size: 12px; }`
  locally, vs. the shared `.btn-sm { min-height: 30px; padding: 5px 10px; font-size: 12px; }`
  — a real padding mismatch.

Net effect: a disabled button (or a small button) looks subtly different depending on
which page it's on, purely as a side effect of independent per-page CSS rather than an
intentional design decision.

## Details
- **Tier**: 3 (code quality / best-practice)
- **Audit date**: 2026-09-17
- **Affected files**: `frontend/pages/music_library.html`, `step1_config.html`,
  `step3_learning.html`, `step4_tts.html`, `step5_video.html`, `step6_thumbnail.html`,
  `step7_youtube.html`, vs. `frontend/static/css/style.css`
- **Verification method**: `wc -l` line counts per page, extracted every inline
  `<style>` block's top-level selectors via `awk`, cross-referenced `.btn`/`.btn-sm`
  variants against the shared stylesheet's actual declared values.

## Risk assessment
Low functional risk (cosmetic only), but a real, user-visible inconsistency, and a
maintenance hazard: future edits to the shared `.btn` styling will silently not apply
on the 4 pages that locally override disabled/small-button styling.

## Scope of the fix (2026-09-18, Task 8.1)

This item bundled a general fragmentation observation (992 lines of per-page inline
CSS vs. 227 in the shared stylesheet) with 3 concrete, verifiable drift examples. Only
the concrete drift was fixed — a full line-by-line audit of all 992 inline lines
across 7 pages for further latent duplication was not attempted, since it wasn't
researched as part of this task and would be a much larger effort disproportionate to
the size of the actual verified problem (matches this project's standing preference
against speculative cleanup with no demonstrated user-visible benefit). Re-open a new
item if a further concrete instance of drift is found later.

## PM Acceptance
Verified 2026-09-18 under Phase 8 / Task 8.1
(`.viepilot/phases/08-css-consolidation/tasks/task-8.1.md`): the shared
`.btn[disabled]` rule in `style.css` now also matches `.btn[aria-disabled="true"]`
(the real gap that caused 3 pages to each invent their own, inconsistent local fix).
`music_library.html`'s and `step7_youtube.html`'s now-fully-redundant local overrides
were removed entirely. `step6_thumbnail.html`'s local rule was reduced to just the one
genuinely non-redundant selector (`button[disabled]`, needed for its bare
`.template-option`/`.variant-card` buttons which have no `.btn` class), corrected to
the shared opacity value; its `.btn-sm` padding override was removed entirely so the
shared value applies. New Playwright tests in `tests/test_quick_wins_browser.py`
verify a real disabled `.btn` and a real `.btn[aria-disabled="true"]` element render
identical computed opacity on every affected page (confirmed via a revert-and-observe
check that the test fails with the pre-fix CSS: `0.55` vs `1`), and that
`step6_thumbnail.html`'s `.btn-sm` padding now matches another page's. 596/598 full
suite passes (2 known Gemini-retry flakes, confirmed non-regressive). **Accepted.**

## Related
- Files: `frontend/static/css/style.css`, `frontend/pages/music_library.html`,
  `frontend/pages/step7_youtube.html`, `frontend/pages/step6_thumbnail.html`
- Cross-referenced Gemini audit finding: "inline CSS fragmentation vs the shared
  stylesheet" — CONFIRMED REAL on independent verification, with concrete drift
  evidence beyond the original report's general claim; the concrete drift is now
  fixed (see "Scope of the fix" above for what remains unaddressed).
