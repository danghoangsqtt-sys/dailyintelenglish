# BUG: Close autosave failure races with browser-level regression coverage

## Meta
- **ID**: BUG-012
- **Type**: Bug
- **Status**: done
- **Priority**: high
- **Created**: 2026-09-11
- **Reporter**: vp-audit (auto-logged)
- **Assignee**: AI
- **Source**: auto-logged by vp-audit Tier 3

## Summary
BUG-008 remains reproducible by inspection. Step 2 has no persistent dirty/error state: after `Api.saveScript` fails, `isSaving` becomes false, the Next handler exits its wait and navigates, and `beforeunload` no longer warns, so edits can be lost. Step 3 re-adds dirty sections after failure but its Next handler busy-waits indefinitely with no retry or recoverable failure result. No Playwright/browser regression tests exist although the request marks that acceptance criterion complete.

## Details
- **Tier**: 3
- **Audit date**: 2026-09-11
- **Affected files**: `frontend/static/js/step2_script.js`, `frontend/static/js/step3_learning.js`, browser E2E test suite

## Acceptance Criteria
- [x] Track unsaved state independently from an in-flight request on Step 2 and Step 3
- [x] Navigation proceeds only after a confirmed successful save
- [x] Save failure leaves the page usable with retry/cancel behavior and no infinite wait
- [x] Regeneration cannot race with dirty or in-flight edits
- [x] Add Playwright/browser tests for success, failure, reload/unload, rapid edits, and navigation races
- [x] Re-run vp-audit: this finding no longer appears

## PM Acceptance
Verified 2026-09-11: 4-state `saveStatus` (saved/dirty/saving/failed) confirmed in both step2_script.js and step3_learning.js, independent of a boolean in-flight flag; failed saves block navigation with Retry; `tests/test_ui_async_browser.py` executed directly — 5/5 pass (normal save+nav, failure-blocks-nav+retry for both steps, rapid-edit coalescence). **Accepted.**

**Fast-follow (non-blocking):** `playwright` is not declared in `requirements.txt` despite being required by this test suite — add it so a fresh clone can reproduce the test run. Log as a small chore, does not block acceptance.

## Related
- Files: `frontend/static/js/step2_script.js`, `frontend/static/js/step3_learning.js`, browser E2E test suite
- Dependencies: BUG-008

## Implementer Evidence (Awaiting PM Acceptance)
1. Implemented explicit 4-state persistence machine (`state.saveStatus`: `"saved"` | `"dirty"` | `"saving"` | `"failed"`) in both `step2_script.js` and `step3_learning.js`.
2. Gated `handleNextStep` so navigation proceeds strictly after save confirmation; save failures block navigation without data loss or infinite loops and prompt the user with a single-click "Retry" action.
3. Disabled single-line regeneration and "Regenerate All" / "Regenerate Pack" whenever unsaved edits exist or saves are in-flight, preventing race conditions.
4. Added `beforeunload` warning when `state.saveStatus !== "saved"` or dirty sections exist.
5. Created Playwright automated headless browser test suite (`tests/test_ui_async_browser.py`) covering 5 real async browser scenarios (success, failure blocking, recovery retry, rapid edits coalescence, and Step 3 tab async safety) — all 5 pass.
