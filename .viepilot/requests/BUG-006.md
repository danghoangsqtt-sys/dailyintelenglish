# BUG: Enforce nonblank project configuration at API boundary

## Meta
- **ID**: BUG-006
- **Type**: Bug
- **Status**: done
- **Priority**: high
- **Created**: 2026-09-11
- **Reporter**: vp-audit (auto-logged)
- **Assignee**: AI
- **Source**: auto-logged by vp-audit Tier 3

## Summary
The UI requires a topic and trims names, but `ScriptConfig` accepts an empty topic and whitespace-only project/speaker names. Direct API clients can persist invalid projects that later fail or generate poor content.

## Details
- **Tier**: 3
- **Audit date**: 2026-09-11
- **Affected files**: `app/models/project.py`, `tests/test_projects_api.py`, `tests/test_project_service.py`

## Acceptance Criteria
- [x] Strip surrounding whitespace for user-entered strings
- [x] Reject blank project name, topic and speaker name at the API boundary
- [x] Add model, service and HTTP regression tests
- [x] Re-run vp-audit: this finding no longer appears

## PM Acceptance
Verified 2026-09-11: `SpeakerConfig.validate_name`, `ScriptConfig.validate_name`/`validate_topic` (`app/models/project.py`) strip and reject blank/whitespace-only strings with 422; `ProjectUpdate` has equivalent optional-field validators plus `forbid_explicit_null`. Whitespace-only topic (`"   "`) confirmed rejected. Tests pass. **Accepted.**

## Related
- Files: `app/models/project.py`, `tests/test_projects_api.py`, `tests/test_project_service.py`
- Dependencies: —

## Implementer Evidence (Awaiting PM Acceptance)
Added validators to `SpeakerConfig`, `ScriptConfig`, and `ProjectUpdate` in `app/models/project.py` that strip surrounding whitespace and reject blank or whitespace-only project names, topics, and speaker names with HTTP 422. Added unit, service, and HTTP API regression tests across `tests/test_projects_api.py` and `tests/test_project_service.py`.
